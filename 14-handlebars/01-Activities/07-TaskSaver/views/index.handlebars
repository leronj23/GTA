<h1>Task Saver</h1>

<h3>Save the tasks you need to get to later</h3>

{{!-- You can iterate over a list using the built-in each helper. 
Inside the block, you can use 'this' to reference the element being iterated over. --}}
<ul>
  {{#each tasks}}
  	<li>
  	   <p>ID: {{this.id}}</p>
       <p>Task: {{this.task}}</p>
  	</li>
  {{/each}}
</ul>

{{!-- action="/" sends the name=task back to the root --}}
<form action="/" method="POST">
	<textarea type="text" name="task"></textarea>

	<button type="submit">Submit</button>
</form>
